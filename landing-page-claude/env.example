# ==========================================
# Landing Page Agent 환경 변수 설정
# ==========================================

# Anthropic API Key (필수)
# https://console.anthropic.com/ 에서 발급받는다
ANTHROPIC_API_KEY=sk-ant-api03-로 시작하는 Claude API Key를 붙혀넣는다.

# ==========================================
# 선택적 설정
# ==========================================

# Claude 모델명
# MODEL_NAME=claude-sonnet-4-5

# 최대 토큰 수 (기본값: 8000)
# MAX_TOKENS=8000

# API 호출 타임아웃 (초, 기본값: 300)
# TIMEOUT=300

# 최대 재시도 횟수 (기본값: 3)
# MAX_RETRIES=3

# 재시도 대기 시간 (초, 기본값: 2.0)
# RETRY_DELAY=2.0

# 메모리 최대 크기 (기본값: 50)
# MAX_MEMORY_SIZE=50

# 로그 레벨 (기본값: INFO)
# 옵션: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# ==========================================
# 경로 설정 (선택사항)
# ==========================================

# Skills 디렉토리 경로
# 기본값: ./skills/landing-page-guide
# SKILLS_DIR=/path/to/skills/landing-page-guide

# 출력 디렉토리 경로
# 기본값: ./output
# OUTPUT_DIR=/path/to/output

# ==========================================
# 사용 예시
# ==========================================

# 1. 이 파일을 복사한다
#    cp .env.example .env

# 2. .env 파일을 열어 ANTHROPIC_API_KEY를 입력한다
#    ANTHROPIC_API_KEY=sk-ant-api03-...

# 3. 필요한 경우 선택적 설정을 추가한다

# 4. Agent를 실행한다
#    python main.py
